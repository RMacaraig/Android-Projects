package com.example.ryle_macaraig_a1;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

//controller
//The controller package should contain “FeedbackController”
// class having insertFeedback() method to add the given feedback
// to the list of existing feedbacks.

public class FeedbackControllerActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_feedback_controller);
    }
}
